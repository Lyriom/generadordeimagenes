# Fijado para que dos despliegues del mismo código sirvan exactamente los mismos
# nombres hash de los chunks JS. Una actualización implícita deja a las pestañas
# abiertas apuntando a módulos que el contenedor nuevo ya no contiene.
streamlit==1.63.0
requests>=2.31,<3.0
Pillow>=10.4,<12.0
streamlit-drawable-canvas>=0.9.3
